# FastAPI - Web framework (handles HTTP + WebSocket)
fastapi==0.109.0

# Uvicorn - Server that runs FastAPI
uvicorn[standard]==0.27.0

# WebSocket support
websockets==12.0

# SQLAlchemy - Database toolkit (works with PostgreSQL)
sqlalchemy==2.0.25

# asyncpg - PostgreSQL driver for async operations
asyncpg==0.29.0

# Pydantic - Data validation (for API request/response)
pydantic==2.5.3

# Redis - Redis client for Python
redis==5.0.1

# Docker SDK - Control Docker from Python
docker==7.0.0

# python-dotenv - Read .env files
python-dotenv==1.0.0

# aiosqlite - SQLite for async (optional, fallback)
aiosqlite==0.19.0